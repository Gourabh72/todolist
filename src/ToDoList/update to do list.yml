info:
  name: update to do list
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/api/v1.0/tasks/{{id}}
  body:
    type: json
    data: |-
      {
        "title": "Learn Kafka",
        "description": "Complete Kafka integration and update the entry",
        "status": "PENDING",
        "priority": "HIGH",
        "dueDate": "2026-06-30"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
