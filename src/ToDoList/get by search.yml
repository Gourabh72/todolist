info:
  name: get by search
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/api/v1.0/tasks/search?title=Learn
  params:
    - name: title
      value: Learn
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
