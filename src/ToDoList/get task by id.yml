info:
  name: get task by id
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/v1.0/tasks/{{id}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
